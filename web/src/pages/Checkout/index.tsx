import React from 'react';
import PaypalButton from '@components/PaypalButton';

const Checkout = () => {
  return(
    <div>
      <PaypalButton />
    </div>
  );
};

export default Checkout;